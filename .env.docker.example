BOT_GOOGLE_ACCOUNT_EMAIL=bot@gmail.com
BOT_GOOGLE_ACCOUNT_PASSWORD=yourpassword
BOT_DISPLAY_NAME=MeetRead AI

GOOGLE_CREDENTIALS_PATH=/app/secrets/credentials.json
GOOGLE_TOKEN_PATH=/app/secrets/token.json

POLL_INTERVAL_SECONDS=300
MAX_MEETING_DURATION_HOURS=3
JOIN_BUFFER_MINUTES=2
LOBBY_WAIT_MINUTES=10
TREAT_REMOVAL_AS_MEETING_END=true

SAVE_MP3=true
DELETE_CHUNKS_AFTER_CONCAT=true

AUDIO_BACKEND=pulseaudio
AUDIO_SAMPLERATE=44100
AUDIO_CHANNELS=2
AUDIO_CHUNK_SECONDS=60

CHROME_BINARY_PATH=/usr/bin/google-chrome
# Leave false for the most reliable Google Meet behavior in Docker. The container
# still has no visible UI because Chrome runs inside an invisible Xvfb display.
CHROME_HEADLESS=false
SKIP_GOOGLE_LOGIN=false
